import React from 'react'

export default () => (
  <>
    <div className="inline-loading" style={{ width: '60%', marginBottom: 'calc(var(--spacing) * 1.25)' }} />
    <div className="inline-loading" style={{ width: '38%' }} />
    <div className="inline-loading" style={{ width: '50%', marginBottom: 'calc(var(--spacing) * 1.25)' }} />
    <div className="inline-loading" style={{ width: '45%' }} />
    <div className="inline-loading" style={{ width: '40%', marginBottom: 'calc(var(--spacing) * 1.25)' }} />
    <div className="inline-loading" style={{ width: '49%' }} />
    <div className="inline-loading" style={{ width: '50%', marginBottom: 'calc(var(--spacing) * 1.25)' }} />
    <div className="inline-loading" style={{ width: '38%' }} />
    <div className="inline-loading" style={{ width: '42%' }} />
  </>
)